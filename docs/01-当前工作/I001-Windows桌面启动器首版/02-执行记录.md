# I001 执行记录

更新时间：2026-08-14 16:12（Asia/Shanghai）

## 已执行

- 读取需求会话、参考项目和上游 DSH 启动链。
- 确认 DSH 支持 `--port 0`、就绪 URL 输出及释放连接。
- 核对本机 Node `24.19.0`、npm `11.7.0`、pnpm `11.20.0` 可用；未安装 Inno Setup。
- 查询 npm 公共源，`@deepseek-ai/dsh@0.1.0-rc.5` 返回 E404。
- 将运行闭包来源由 npm 包切换为固定上游提交构建。
- 按新版全局规范删除旧格式本地文档并重建当前结构。
- 将本地 `origin` 更新为 MichengAI 规范地址，并创建 `codex/windows-launcher-mvp` 分支；未推送。
- 复用 `D:\Repository\deepseek-harness` 的既有构建；确认其 DSH Web 启动后返回 HTTP 200。
- 实现 Electron TypeScript 启动器及 3 个就绪地址、Node 选择单元测试。
- 用 `pnpm deploy --legacy` 导出生产依赖并扁平化已构建工作区包，随包 Node 24 启动 DSH 返回 HTTP 200。
- 完成打包后 exe 冒烟：随包 Node 与 DSH 子进程启动，本机 HTTP 返回 200。
- 生成 `DeepSeek Harness Desktop-0.1.0-x64.zip` 和当前用户 NSIS 安装器，并记录 SHA256。
- 提交首版可运行基线 `9eb02a8`，未推送。
- 修复数据目录、外链打开、HTTP 健康检查、运行中断提示、随包 Node 缺失回退和日志敏感输出问题。
- 新增引导脚本，以 IPC 触发 DSH 既有优雅关闭；超时后仅强制结束本应用创建的 PID。
- 新增分片就绪、提前退出、超时、重复关闭和导航安全测试；本地 10 项测试通过。
- 将 Electron Builder 输出迁移至 `release\`，ASAR 仅包含 `dist\src\**`，并验证不含测试和构建脚本。
- 固定 Node `24.19.0`、pnpm `11.20.0` 与 Node SHA256，并在装配时校验。
- 新增 CI 打包后冒烟：验证根页面、前端资源和应用退出后的引导进程清理；标签发布时仅上传 Release 附件，不上传 Actions 制品。
- 已重新生成 ZIP 和当前用户 NSIS 安装器，并完成未安装目录冒烟。

## 未执行

- 未执行发布修复提交或推送。
- 未执行干净 Windows 安装、卸载和 CI 验收。
