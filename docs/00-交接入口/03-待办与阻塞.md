# 待办与阻塞

更新时间：2026-08-19 18:45（Asia/Shanghai）

| 事项 | 负责人 | 优先级 | 下一步 | 解除条件 |
|---|---|---|---|---|
| 生成含预装官方运行时的安装包 | 桌面端 | 高 | 执行 `pnpm run dist` | 安装包 `resources\\dsh-runtime` 存在，且新用户首启复制后不再现场 `pnpm add @deepseek-ai/dsh` |

Apple 签名与公证凭据为后续正式发布的可选项，不阻塞测试版发布。
