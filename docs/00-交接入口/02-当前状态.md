# 当前状态

更新时间：2026-08-14 18:55（Asia/Shanghai）

## 当前阶段

Windows `v0.1.0` 已发布。Windows x64、macOS Apple Silicon 和 macOS Intel 的远端构建、打包和冒烟均已通过；等待 Apple 签名与公证凭据进行 macOS 标签发布验收。

## 活动迭代

`I002-macOS桌面端支持`：三平台远端验证全部通过；公开 macOS 发布受 Apple 凭据阻塞。

## 有效风险

- 缺少 Apple Developer 签名与公证凭据时，macOS 标签发布会被 CI 阻止。
