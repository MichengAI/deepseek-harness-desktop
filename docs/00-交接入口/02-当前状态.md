# 当前状态

更新时间：2026-08-20 21:53（Asia/Shanghai）

## 当前阶段

当前 `main` 版本基线为 `v1.0.8`。六个功能插件与 dshmarket 已纳入离线仓库；评审列出的运行时、插件生命周期、归档完整性和发布签名问题已完成代码修复。Windows x64 解包产物已通过桥接导入、归档真实解压和桌面启动冒烟，待 macOS/Linux CI 验证后发布下一补丁版。

## 活动迭代

`I003-内置社区插件与在线安装兼容`

## 有效风险

- macOS/Linux 尚未在本机验证，必须以对应 CI 打包与冒烟结果为准。
- 正式标签发布必须配置 Windows 签名以及 macOS 签名、公证凭据，缺失时 CI 会主动阻断。
- 离线插件仓库和预装官方运行时只在 `prepare-runtime` 后生成；未装配时不会补种，也不阻断启动。
- v1.0.8 已发布的产物不包含本次评审修复，必须用后续补丁版本重新打包发布。
