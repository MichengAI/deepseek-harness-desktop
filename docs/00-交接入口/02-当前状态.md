# 当前状态

更新时间：2026-08-20 20:44（Asia/Shanghai）

## 当前阶段

当前 `main` 版本基线为 `v1.0.8`。六个功能插件与 dshmarket 已纳入离线仓库；发布阻断评审发现的桥接闭包、更新元数据、可写目录回退和进程清理问题已完成代码修复，待下一补丁版打包验证。

## 活动迭代

`I003-内置社区插件与在线安装兼容`

## 有效风险

- macOS 测试包未配置签名与公证，Gatekeeper 可能要求用户手动确认来源。
- 离线插件仓库和预装官方运行时只在 `prepare-runtime` 后生成；未装配时不会补种，也不阻断启动。
- v1.0.8 已发布的产物不包含本次评审修复，必须用后续补丁版本重新打包发布。
