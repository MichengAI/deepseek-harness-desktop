# 当前状态

更新时间：2026-08-14 19:16（Asia/Shanghai）

## 当前阶段

Windows `v0.1.0` 已发布。Windows x64、macOS Apple Silicon 和 macOS Intel 的远端构建、打包和冒烟均已通过；当前将发布未签名、未公证的 macOS 测试版。

## 活动迭代

`I002-macOS桌面端支持`：三平台远端验证全部通过，正在进行未签名 macOS 测试版发布。

## 有效风险

- 未签名 macOS 测试包会被 Gatekeeper 拦截，需要用户确认来源后手动允许打开。
