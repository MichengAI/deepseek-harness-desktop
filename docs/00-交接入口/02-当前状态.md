# 当前状态

更新时间：2026-08-20 17:00（Asia/Shanghai）

## 当前阶段

`v1.0.0` 正式版版本号已初始化，待打包并创建发布标签。正在把六个社区插件打进桌面端，并保持 code-ui / dshmarket 在线安装兼容。

## 活动迭代

`I003-内置社区插件与在线安装兼容`

## 有效风险

- macOS 测试包未配置签名与公证，Gatekeeper 可能要求用户手动确认来源。
- 离线插件仓库和预装官方运行时只在 `prepare-runtime` 后生成；未装配时不会补种，也不阻断启动。
- 当前已安装的桌面端还是旧包，预装官方运行时、最大化、任务栏大图标和新用户数据目录要重新 `pnpm run dist` 后才会生效。
